tuto de  https://github.com/mevedprakash


Json to typscript
https://transform.tools/json-to-typescript
extension ==> https://marketplace.visualstudio.com/items?itemName=quicktype.quicktype


https://muhimasri.com/blogs/create-an-editable-dynamic-table-using-angular-material/

tuto de  https://github.com/mevedprakash

https://levelup.gitconnected.com/angular-signals-in-practice-part-1-6cdbcc2a0bff

https://dummyjson.com/


  latList = computed(() =>
    this.usersJson().users.map((u) => u.address.coordinates.lat)
  );

  lngList = computed(() =>
    this.usersJson().users.map((u) => u.address.coordinates.lng)
  );

  locations = computed(() => {
    return this.usersJson().users.map((user) => ({
      lat: user.address.coordinates.lat,
      lng: user.address.coordinates.lng,
      city: user.address.city,
    }));
  });


ng g s services/login/login-service
ng generate interceptor interceptors/auth-token
ng g guard guards/is-logged-in

